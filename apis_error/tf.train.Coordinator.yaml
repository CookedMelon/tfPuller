args:
  clean_stop_exception_types: Optional tuple of Exception types that shouldcause a
    clean stop of the coordinator. If an exception of one of thesetypes is reported
    to request_stop(ex) the coordinator will behave asif request_stop(None) was called.  Defaults
    to(tf.errors.OutOfRangeError,) which is used by input queues to signalthe end
    of input. When feeding training data from a Python iterator itis common to add
    StopIteration to this list.
error_message: Multiple args
name: tf.train.Coordinator
url: https://www.tensorflow.org/api_docs/python/tf/train/Coordinator
warn_message:
- arg_name or arg_desc too long
